#include <stdio.h>

/**
 * main - prints alpha in lower case
 * Return: Always0 (Success)
 */

int main(void)
{
	char lc;

	for (ls = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
